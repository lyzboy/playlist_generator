var assert = require("assert");

// create a basic mocha test to see if searchbar component is rendering
describe("SearchBar", function () {
    it("should render", function () {
        assert.equal(1, 1);
    });
});
// Run the test with the following command:
// npm test
